// select the thing
document.querySelector('.login-form').addEventListener('submit', function(evt){
    console.log(evt.target.elements);
    var username = evt.target.elements.username.value;
    var password = evt.target.elements.password.value;
    var valid;
if (valid){
    evt.preventDefault();
}
});
// create a function to enter the form

// make an eventListener

// do task